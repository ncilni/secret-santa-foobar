{
  "name": "foobar-secret-santa-server",
  "version": "0.0.0",
  "engines": {
    "node": "12.16.3"
  },
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && ng build --output-path ../dist"
  },
  "private": true,
  "dependencies": {
    "body-parser": "^1.19.0",
    "core-js": "^2.5.4",
    "express": "^4.17.1",
    "mongodb": "^3.5.7",
    "nodemailer": "^6.4.6"
  },
  "devDependencies": {}
}
